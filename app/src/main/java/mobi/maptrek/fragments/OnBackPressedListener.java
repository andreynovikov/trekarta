package mobi.maptrek.fragments;

public interface OnBackPressedListener {
    /**
     * Called when back button is pressed
     * @return <code>true</code> if fragment wants to handle back press
     */
    boolean onBackClick();
}
